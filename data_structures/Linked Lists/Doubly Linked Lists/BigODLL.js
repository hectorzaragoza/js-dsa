// Big O of DLL

/**
 * Insertion  O of 1
 * Removal O of 1
 * Searching O of N
 * Access O of N
 * 
 * DLL are almost identically to SLL except there is an additional pointer to previous nodes
 * DLL better than SLL for finding nodes and can be done in half the time
 * However, they do take up more memory considering the extra pointer
 */